import React from 'react';

// MapLibre native module is not available in this build.
// EEZ boundary is shown via the BorderDetailPanel and ZoneStatusBadge instead.
const BorderAlertOverlay: React.FC = () => null;

export default BorderAlertOverlay;
